# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
env/
.env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Data (large files)
data/*.csv

# Model weights (keep in results for this project)

# The fitted scalers. train.py writes them, and nothing in this repository reads
# them back: every entry point refits them from the seeded data in under a
# second, so the committed copies were a pair of pickle files with no consumer.
results/scalers/
