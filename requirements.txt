# Pinned so a reported score can be reproduced. Versions are taken from
# installed distribution metadata and the set is resolved as a whole before
# being committed, not transcribed one line at a time.
joblib==1.5.3
matplotlib==3.10.8
numpy==2.4.3
pandas==2.3.3
scikit-learn==1.8.0

# torch is deliberately left as a lower bound. The build differs by accelerator
# and comes from a different index for CUDA, XPU or CPU. Development used
# 2.11.0+xpu; CI installs the CPU build.
torch>=2.1
